# Core package for YOLO models (includes torch, torchvision, etc. as dependencies)
ultralytics>=8.0.0
scikit-learn # for data preprocessing

# Visualization & plotting
matplotlib>=3.5.0
seaborn>=0.11.0 # optional


# Data handling
numpy>=1.20.0

# Jupyter & environment tools
jupyter>=1.0.0 # if running in notebook
tqdm>=4.60.0 # progress bars (used by ultralytics)

# for fetching data from the internet
requests>=2.25.0 # for downloading models/datasets